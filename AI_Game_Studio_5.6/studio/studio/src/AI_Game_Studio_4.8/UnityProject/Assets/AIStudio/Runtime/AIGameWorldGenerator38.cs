using UnityEngine;

namespace AIStudio {
    public class AIGameWorldGenerator38 : MonoBehaviour {
        public string projectId;
        public string worldId;

        public void Configure(string project, string world) {
            projectId = project;
            worldId = world;
        }
    }
}
