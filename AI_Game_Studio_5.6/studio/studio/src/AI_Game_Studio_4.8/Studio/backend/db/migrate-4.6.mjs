import 'dotenv/config';import pg from 'pg';import fs from 'node:fs/promises';import path from 'node:path';const{Pool}=pg;if(!process.env.DATABASE_URL){console.log('DATABASE_URL is not set; nothing to migrate.');process.exit(0)}const pool=new Pool({connectionString:process.env.DATABASE_URL,ssl:process.env.DATABASE_URL.includes('localhost')?false:{rejectUnauthorized:false}});const sql=await fs.readFile(path.join(process.cwd(),'db/schema-4.6.sql'),'utf8');await pool.query(sql);await pool.end();console.log('AI Game Studio 4.6 database ready.');
