AI Game Studio 3.3 Asset Layer
AssetManifest.json contains isolated, project-scoped asset jobs.
status=spec_ready means the job has a structured generation specification, not a generated 3D/image/audio file.
External generation providers can later consume each jobId and write results into the manifest folders.
