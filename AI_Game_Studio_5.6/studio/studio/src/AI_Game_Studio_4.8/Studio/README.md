# AI Game Studio 4.9

AI-driven gameplay compiler and playable Web Preview. 4.9 takes the current project's Blueprint and derives a concrete gameplay feature profile, then the preview activates matching mechanics instead of using a single hard-coded demo.

Start: `npm start` from `Studio/backend`. The web UI is served from `Studio/web/4.9`.
