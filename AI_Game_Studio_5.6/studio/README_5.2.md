# AI Game Studio 5.2 — Unified Generation API

5.2 connects the web launch flow to a single project build orchestration API. A project can be created and then built through one endpoint. The build writes project-scoped artifacts and a BUILD_MANIFEST.json.

This package is a deployable application layer, not a claim that Unity/Apple/Google cloud credentials are included. Real production builds still require configured infrastructure and provider credentials.
